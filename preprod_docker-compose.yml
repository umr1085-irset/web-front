version: '3'

services:

  genoapp-front-static-prod:
    image: scilicium/geno_app-front:dev
    container_name: genoapp_front-static-prod
    networks:
      - web
      - genoapp

networks:
  web:
    external: true
  genoapp:
    external: true
